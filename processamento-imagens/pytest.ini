[pytest]
# Executar a partir de processamento-imagens/:
#     .venv/Scripts/python.exe -m pytest
#
# rootdir aqui garante que `src` seja importável como pacote sem instalação.
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --tb=short
